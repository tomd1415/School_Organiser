# Sensing inputs — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will change a variable using buttons and movement, and show the variable's value.

## Changing a variable with buttons
A **variable** holds a value that can change. Here a variable called *count* goes up by 1 when button A is pressed, and down by 1 when button B is pressed.

![Code: if button A change count by 1, if button B change count by -1]({{res:l3-counter-buttons.png}})

## Changing a variable by moving
The **accelerometer** senses movement. The *is shake gesture* block lets a shake change the count too.

![Code with is shake gesture showing the count]({{res:l3-counter-shake.png}})

Put the blocks in the right order to make the counter.

```parsons
forever
if button A is pressed then
change count by 1
if button B is pressed then
change count by -1
if is shake gesture then
show number count
```

## 🟢 Support
| Question | Choose |
|---|---|
| The accelerometer senses… | (  ) movement (  ) colour (  ) sound |

## 🟡 Core
| Question | Choose |
|---|---|
| When the program shows the count, the value of count… | (  ) stays the same (  ) goes back to 0 (  ) doubles |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Explain why checking (showing) a variable does not change its value. | Type your answer here |

## Show your work
Make the counter in MakeCode. Test it on the emulator with the buttons and with a shake.

| Question | Your answer |
|---|---|
| Paste your MakeCode share link here | Type your answer here |
| Show your finished program | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I can use a button press to change a variable
- [ ] I can use movement (the accelerometer) to change a variable
- [ ] I can show a variable, and know that checking it does not change its value
