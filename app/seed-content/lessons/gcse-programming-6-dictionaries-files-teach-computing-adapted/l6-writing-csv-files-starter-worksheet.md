# Writing to CSV files — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will **write** a list to a **CSV file** using `join()`.

## Starter — think, write, pair, share
Last lesson you **read** from CSV files with `split()`. Today you will **write** to them with `join()`.

| Question | Your answer |
|---|---|
| When might you choose a **CSV file** instead of a plain text file? Write one idea. | Type your answer here |

## 🟢 Support
| Question | Choose |
|---|---|
| You would use a CSV file when data needs to be in… | (  ) rows and columns (a table) (  ) one long sentence (  ) a picture |
| Before you can write a number to a file, you must change it to a… | (  ) string (  ) list (  ) boolean |

## 🟡 Core
| Question | Your answer |
|---|---|
| `split()` breaks a string into a list. What do you think `join()` does? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Why does each ROW of a CSV file need a `\n` at the end when you write it? | Type your answer here |
