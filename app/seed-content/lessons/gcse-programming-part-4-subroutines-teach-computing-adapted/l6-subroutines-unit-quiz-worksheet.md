# Subroutines unit quiz

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
This quiz checks everything from the subroutines unit: **subroutines, parameters, functions, scope, XOR, structured programming, and testing**.

## Key words — match each to its meaning
| Term | Meaning |
|---|---|
| Parameter | (  ) a name inside a subroutine that receives a value (  ) a value passed in when you call it (  ) sends a value back out (  ) a value that should not change |
| Argument | (  ) a name inside a subroutine that receives a value (  ) a value passed in when you call it (  ) sends a value back out (  ) a value that should not change |
| Return | (  ) a name inside a subroutine that receives a value (  ) a value passed in when you call it (  ) sends a value back out (  ) a value that should not change |
| Constant | (  ) a name inside a subroutine that receives a value (  ) a value passed in when you call it (  ) sends a value back out (  ) a value that should not change |

## Sort the test data
```sort
Normal data: a sensible number like 5, an expected surname, a value in the middle of the range
Boundary data: the lowest allowed value, the highest allowed value
Erroneous data: a letter instead of a number, an empty answer
```

## 🟢 Support
| Question | Choose |
|---|---|
| A subroutine that **returns a value** is called a… | (  ) function (  ) procedure (  ) loop |
| A variable made INSIDE a subroutine is… | (  ) local (  ) global (  ) a constant |
| Structured code has one entry point and one… | (  ) exit point (  ) error (  ) parameter |

## 🟡 Core
Tick **all** the statements that are true about a **function**.

| Question | Tick all that are true |
|---|---|
| Which of these are true about a function? | [ ] it returns a value with return [ ] it can have parameters [ ] code after return still runs [ ] you can call it many times |

Fill in the gaps:

XOR is True when its two inputs are [[ ]].

A value written in CAPITALS to show it should not change is a [[ ]].

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write a function `double(a)` that **returns** `a` times 2 (do not print inside it). Type your program. | Type your code here |
| Rewrite this so it has ONE exit point: `def is_even(n): if n % 2 == 0: return True else: return False`. Type your program. | Type your code here |

## How confident do you feel?
| Question | Your answer |
|---|---|
| How confident are you with subroutines and functions now? | [scale 1-5: not sure … very confident] |

## Show your work
| Question | Your answer |
|---|---|
| Show your finished quiz code | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I know what a subroutine, parameter and argument are
- [ ] I can tell a function from a procedure
- [ ] I can explain local and global scope
- [ ] I can describe what XOR does
- [ ] I can explain the structured approach and the three types of test data
