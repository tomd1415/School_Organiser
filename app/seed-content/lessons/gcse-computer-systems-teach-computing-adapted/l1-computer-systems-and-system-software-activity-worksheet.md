# Computer systems and system software — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will describe what system software does and name the parts inside a computer.

## The start-up sequence — name the hardware
When you press the power button, the hardware wakes up. Match each part to what it does. Pick the matching part on every row.

| What it does | Match the part |
|---|---|
| The brain of the computer — it processes instructions | (  ) CPU (  ) RAM (  ) ROM (  ) Hard drive |
| Fast memory that holds programs while they are running (it is emptied when you switch off) | (  ) CPU (  ) RAM (  ) ROM (  ) Hard drive |
| Memory that keeps the start-up instructions even with the power off | (  ) CPU (  ) RAM (  ) ROM (  ) Hard drive |
| Storage that keeps your files when the power is off | (  ) CPU (  ) RAM (  ) ROM (  ) Hard drive |

![A computer chip (the CPU)]({{res:l1-a-computer-chip-the-cpu-teach-computing.png}})

## What is system software?
System software runs the computer itself. The most important piece is the OPERATING SYSTEM (OS). Another type is UTILITY SOFTWARE (small tools that look after the computer).

## 🟢 Support
| Question | Choose |
|---|---|
| The most important piece of system software is the… | (  ) operating system (  ) web browser (  ) game |
| Antivirus and backup tools are examples of… | (  ) utility software (  ) hardware (  ) the CPU |

## 🟡 Core
The operating system does important jobs. Fill in the gaps.

The operating system manages the [[ ]] (hardware like the keyboard and printer). It also manages [[ ]] so each program gets a turn on the CPU. It gives you a way to control the computer called the user [[ ]].

## 🔴 Challenge
Sort each job into the right group: is it done by the OPERATING SYSTEM, or is it a UTILITY SOFTWARE tool?

```sort
Operating system: manages memory, controls hardware devices, gives a user interface
Utility software: antivirus scanning, backing up files, compressing files
```

## Show your work
| Question | Your answer |
|---|---|
| Write one embedded system you found in the classroom | Type your answer here |
| Show your finished notes | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what a computer system is (input, process, output)
- [ ] I compared a general purpose computer and an embedded system
- [ ] I described what system software does
- [ ] I described the role of the operating system and named utility software
