# The fetch-decode-execute cycle — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
First I will remember the CPU parts, then I will learn how the CPU runs an instruction.

## Starter — remember the CPU
Match each CPU part to its job. Pick the matching part on every row.

| Job | Match the part |
|---|---|
| Does the maths and logic | (  ) ALU (  ) Control unit (  ) Clock |
| Tells the other parts what to do | (  ) ALU (  ) Control unit (  ) Clock |
| Sets the speed with regular pulses | (  ) ALU (  ) Control unit (  ) Clock |

## 🟢 Support
| Question | Choose |
|---|---|
| A register is a… | (  ) tiny fast store inside the CPU (  ) big slow hard drive (  ) printer |

## 🟡 Core
| Question | Your answer |
|---|---|
| Where are a program's instructions held, ready for the CPU to use them? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Why does the CPU need fast registers instead of fetching every value from RAM each time? | Type your answer here |
