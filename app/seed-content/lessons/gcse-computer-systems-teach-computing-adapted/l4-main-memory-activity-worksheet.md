# Main memory — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will compare RAM and ROM, and describe what cache memory does.

## RAM and ROM
RAM and ROM are both MAIN MEMORY — the memory the CPU works with directly.

![RAM memory sticks]({{res:l4-ram.png}})

## Sort the facts
Drag each fact onto RAM or ROM.

```sort
RAM: loses its data when the power is off, holds programs while they are running, can be read from and written to
ROM: keeps its data when the power is off, holds the start-up instructions, can only be read from
```

## Volatile or non-volatile?
VOLATILE memory loses its data when the power is off. NON-VOLATILE memory keeps its data. Fill in the gaps.

RAM is [[ ]], so it loses everything when you switch off. ROM is [[ ]], so it keeps the start-up instructions even with the power off.

## Cache
![Memory chips on a circuit board]({{res:l4-cache-chips.png}})

Cache is a small amount of very fast memory. It sits between the CPU and RAM and holds the data the CPU uses most.

## 🟢 Support
| Question | Choose |
|---|---|
| Which memory loses its data when the power is off? | (  ) RAM (  ) ROM |
| Cache memory is… | (  ) very fast and very close to the CPU (  ) a slow hard drive (  ) a printer |

## 🟡 Core
| Question | Your answer |
|---|---|
| What does cache memory do? | Type your answer here |
| Give one difference between RAM and ROM. | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Explain how cache speeds up a computer system. Use the words "fast", "CPU" and "RAM". | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Write whether RAM is volatile or non-volatile | Type your answer here |
| Show your finished main memory notes | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what RAM does and that it is volatile
- [ ] I described what ROM does and that it is non-volatile
- [ ] I compared RAM and ROM
- [ ] I defined cache and described what it does
