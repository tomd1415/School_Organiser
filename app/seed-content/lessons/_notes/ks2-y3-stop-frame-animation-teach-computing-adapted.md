# Conversion notes — KS2 Y3 Stop-frame animation (Teach Computing — adapted)

- **Slug:** `ks2-y3-stop-frame-animation-teach-computing-adapted`
- **Course:** KS2 Computing · KS2
- **Source:** TeachComputing/KS2/Year_3/Unit 2 Stop-frame animation (unit overview + L1–L6 zips)
- **Lessons converted:** 6 of 6 (all)
- **Cohort:** SEND secondary working at primary levels — content concrete & simple, framing age-respectful (no infant tone), very low reading load, heavy use of visual/drag types. Animation has motion: all source GIFs/videos referenced as **teacher-played only**; slides kept low-arousal.

## Per-lesson alignment (TCC objective → native lesson)

| L | TCC lesson | TCC objective(s) | Native lesson | Key activity types | Image |
|---|---|---|---|---|---|
| 1 | Can a picture move? | Animation is a sequence of drawings/photos; make a flip book | Can a picture move? — flip books | order (wave frames), single-choice (where/which sheet), multi-select (flip-book facts), screenshot | flip-book sticky notes |
| 2 | Frame by frame | Relate movement to a sequence of images; make a stop-frame animation | Frame by frame — using the animation app | order (start-up steps), **label-a-diagram** (app: capture/stop/onion skin), single-choice, screenshot | app home + capture screen |
| 3 | What's the story? | Break a story into settings/characters/events; storyboard; keep it achievable | What's the story? — storyboard | **card-sort** (setting/character/event), order (start/middle/end), single-choice (easy plan), screenshot | a scene (setting + characters) |
| 4 | Picture perfect | Work consistently & carefully; onion skinning; review frames | Picture perfect — small careful changes | matching (problem→fix), multi-select (keep the same), spot-the-change single-choice (2 frames), order (careful steps), screenshot | two near-identical frames |
| 5 | Evaluate and make it great | Review & improve; evaluate; delete frames | Evaluate and make it great | checklist (good animation), **slider 1–5** (rate), single-choice (delete a frame), screenshot | watch-back screen |
| 6 | Lights, camera, action | Add other media; evaluate impact | Lights, camera, action — music & titles | card-sort (media/not media), **label-a-diagram** (movie app: Video/Photos/Audio), single-choice, screenshot | movie-app media panel |

Each lesson: 3 "I can…" objectives, identical routine outline (recap → starter → I-do/we-do → you-do → plenary), Support/Core/Challenge on the SAME task, vocabulary list, likely-error + fix-words, TA cues. No pupil is ever named.

## Image-gap log
- **L1 — no still frames of the "wave" animation.** TCC supplies it as a video only, so the order task uses three described positions (arm down/halfway/up) rather than embedded stills. *Gap: 3 simple wave-frame stills would let the order task be image-based.*
- **L2/L4/L5 — source "making your animation" / "spot the change" / "watch back" demos are videos** (>6 MB). Referenced as teacher-played hooks; single representative app screenshots embedded instead. *Gap: a still per "spot the change" clip (character swap, tablet moved, background change) would support an image-based consistency task in L4.*
- **L3 — characters/backgrounds are Roman-themed clipart** (El Djem colosseum scene). Used one clean scene still for the setting/character sort; individual character cards not embedded (kept load low). *No blocking gap.*
- Decorative source clipart (Minion, genie lamp, monkey, onion, travel-advert footage) deliberately skipped.

## Type-gap log
- Types used across the unit: order, label-a-diagram, card-sort, matching, single-choice, multi-select, slider/scale, checklist, screenshot, text. Strong coverage of the brief's "strong fits".
- **Code / Parsons / fill-blank deliberately not used** — this is a creative media unit with no text-code or cloze content that would suit them; forcing them would be artificial. Not a gap.

## Self-verify
- `renderWorksheet` (preview + support/challenge slices), `sliceSlidesForLevel`, `splitTeacherNotes` over all 6 lessons: **PASS** — every activity worksheet has a screenshot (image) field, support ≠ challenge slices, every deck ≥ 4 slides with non-empty teacher notes, and every `{{res:}}` placeholder resolves to a manifest file.
