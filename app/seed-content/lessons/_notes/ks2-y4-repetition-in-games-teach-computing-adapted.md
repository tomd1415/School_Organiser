# Conversion notes — KS2 Y4 Repetition in games (Teach Computing — adapted)

- **Slug:** `ks2-y4-repetition-in-games-teach-computing-adapted`
- **Course / key stage:** KS2 Computing / KS2
- **Source:** TeachComputing KS2 Year 4, Unit 6 "Repetition in games" (6 lessons, Scratch).
- **Cohort:** SEND secondary working at KS2 levels — primary content, age-respectful framing, very low reading load, lean on drag/visual question types.

## §7a Alignment tables (objective → slide → worksheet question)

### L1 Using loops to create shapes
| Objective | Slide | Worksheet question |
|---|---|---|
| list an everyday job as steps that repeat | S2 Things that repeat | starter sort (repeats/once); activity `sort` jobs |
| read a loop and predict what it draws | S3 Scratch screen, S4 repeat block | activity `label` the screen; Support choose sides; Core match repeat→shape |
| change a loop to draw a shape | S5 your turn | activity Challenge blank (triangle turn) + Show-your-work screenshot |

### L2 Different loops
| Objective | Slide | Worksheet question |
|---|---|---|
| tell forever vs count-controlled | S2 forever, S3 repeat | starter match; activity `sort` forever/count |
| change a loop for an outcome | S4 change a loop | Core blank (repeat 5) + screenshot |
| choose the right loop | S2–S3, S5 two at once | activity match loop→job; Support choose; Challenge explain |

### L3 Animate your name
| Objective | Slide | Worksheet question |
|---|---|---|
| choose an action to repeat per letter | S3 blocks | starter choose; activity Support choose repeat block |
| say what the repeated action does | S2/S3 | activity `order` build steps; Core match action→outcome |
| evaluate the animation | S5 evaluate | activity `[scale 1-5]` + "one change" + Show-your-work |
| (key idea) two+ loops at once | S4 all at the same time | Challenge blank "at the same time" |

### L4 Modifying a game
| Objective | Slide | Worksheet question |
|---|---|---|
| find parts of a loop to change | S3 what can change | activity `sort` faster/slower |
| say what my change does | S5 your turn | Core blank effect + Show-your-work before/after |
| re-use code on a new sprite | S4 re-use | Core blank "new"; Challenge explain |

### L5 Designing a game
| Objective | Slide | Worksheet question |
|---|---|---|
| say where repetition is used | S2 model game | starter choose what repeats |
| pick parts of a model game | S3 good design | design worksheet `order` steps + partner share |
| write a simple design | S4 your algorithm | design table (sprite/loop) + Show-your-work |

### L6 Creating our games
| Objective | Slide | Worksheet question |
|---|---|---|
| follow my design to build | S2/S3 follow algorithm | starter `order` build steps |
| find and fix a mistake | S4 test and fix | Support choose; Core blank "forever" + what you fixed |
| say how well my game works | S5 evaluate, S6 showcase | `[scale 1-5]` + went well/improve + Show-your-work |

All objectives are taught on a slide and assessed on a worksheet; no orphan questions. All multiple-choice cells are single-correct.

## Question types used
order, sort (card-sort), label-a-diagram (L1 Scratch screen, 5 zones), matching/choice, fill-in-the-blank, scale (self-rate), single-choice, screenshot (every activity), checklist. Good spread of the new drag types — strong fit for the SEND cohort.

**Type gaps:** none. No new type wanted that the engine lacks.

## Images embedded (all OGL — Teach Computing © Raspberry Pi Foundation, OGL v3.0)
Real screenshots/blocks were available and used:
- L1: clock (real-life repetition), the **Scratch screen** (used for the `label` task), repeat-4 square block.
- L2: **forever loop** block, **repeat (count-controlled)** block — clean contrast pair.
- L3: animated name on the stage, Looks blocks panel.
- L4: shark game stage.
- L5: model bat game stage.
- L6: bat game open in the Scratch editor (4 sprites).

**Image gaps:** none blocking. Soft wants:
- L3 a clean "two letters animating at the same time" still (used the static stage + Looks panel instead).
- L6 a before/after "bug → fixed" still would help the test-and-fix slide (none clean in source).
Source decks also contain decorative clipart (pencils, plate, lungs, sunrise, beetle, ballerina) and one animated GIF (excluded per no-animation rule) — not embedded.

## Self-verify
PASS — for every lesson: activity worksheet has a screenshot (`kind:'image'`) field and support≠challenge slices; slides ≥4 with non-empty teacher notes; every `{{res:}}` resolves to a manifest file present on disk; all fill-blanks use the empty `[[ ]]` marker (content-in-brackets bug caught and fixed); slides resource titles end `.md`.
