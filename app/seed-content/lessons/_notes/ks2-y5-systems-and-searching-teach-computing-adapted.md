# Conversion notes — KS2 Y5 Systems and searching (Teach Computing — adapted)

**Slug:** `ks2-y5-systems-and-searching-teach-computing-adapted`
**Course / key stage:** KS2 Computing · KS2
**Lessons:** 6 (every TCC lesson converted)
**Framing:** PRIMARY content, SEND-secondary cohort — simple/concrete, age-respectful (not babyish),
very low reading load, lean on visual/drag types (label, sort, order, multi-select, screenshot).

## Question-type spread (variety, low writing)
- **label** — L1 starter (bike parts), L6 activity (find the Sponsored / Ad spots on a real results page)
- **sort** (card-sort) — L1 activity (input/process/output), L2 activity (person vs computer job), L3 activity
  (town vs other meaning), L6 starter (searcher / search engine / page maker)
- **order** (sequence) — L1 activity (teddy steps), L2 activity (order an order), L3 activity (search steps),
  L4 activity (crawl→index→search), L5 starter (rank cities by size)
- **multi-select** — L2 (systems; private info), L3 (search engines; trust signs), L4 (good ways to find a page),
  L5 (ranking factors), L6 (true advert facts; search limits)
- **single-choice / matching grids** — every starter Support section (single-correct; not the multi-correct trap)
- **screenshot** — every activity worksheet (📷 show-your-work)
- **scale** — not used (no natural confidence-rating point; checklist covers self-assessment)

## Image use (all OGL, Teach Computing © Raspberry Pi Foundation)
| Lesson | Embedded | Used for |
|---|---|---|
| L1 | bicycle, washing machine, talking teddy, smart locker | label the system; input/process/output; design a system; computers talking |
| L2 | delivery van | the shop (catalogue store) system |
| L3 | Rugby town, Sale town, Boot village | refine-a-search (place names with two meanings) |
| L4 | web-crawler spider, data centre | what a crawler is; where the index lives |
| L5 | search bar "your school", school building | how ranking works; make-a-page activity |
| L6 | Google "guitar" results with Sponsored + Ad | label the adverts; how search engines make money |

## §7a Alignment (objective → slide → worksheet Q)
**L1 Systems** — system=parts (S2 / starter label + Support match); input/process/output (S3 / activity sort +
Support choice); computers talk (S5 / activity locker Q). Make-a-system → activity order + screenshot.
**L2 Systems & us** — name systems (S2 / starter multi-select); sort person/computer (S4 / activity sort +
Support choice); a benefit (S3 / activity sensor Q); stay safe (S5 / activity multi-select).
**L3 Searching** — use a search engine (S2 / starter multi-select + activity order/search-Q); refine (S4 /
activity sort + S/C/C); compare + trust (S5 / activity trust multi-select).
**L4 Selecting** — why we need search engines (S2 / starter multi-select); crawler (S4 / activity Core Q +
Support match); index (S3+S5 / activity order + Challenge Q).
**L5 Ranked** — rank order (S2 / starter order); rules/score (S3 / activity score Q + Support choice);
factors (S5 / activity multi-select + Core/Challenge).
**L6 Influenced** — ways changed (S2 / starter sort); make money (S4 / activity Core/Support + multi-select);
limits (S5 / activity limits multi-select). End-of-unit recap on final slide.
Every objective is on a slide AND has ≥1 worksheet question; every question maps to a slide; choices single-correct.

## Image-gap log (carry to WORKSHEET_QUESTION_TYPES.md §4 next pass)
- **L2** — no puffin/pedestrian-crossing raster in source (PPT shapes); a clean crossing-with-sensor photo would
  unlock a label task. Used the delivery-van clipart for the shop system instead.
- **L3** — search-engine LOGO strip and the "search page vs address bar (omnibox)" still are PPT shapes / a
  >6 MB video (`AP Resource - Video (search page vs address bar).mp4`, not embedded). A clean omnibox screenshot
  would help the "two ways to search" point.
- **L5** — a real ranked search-results screenshot (multiple results stacked) would beat the single search-bar
  still; source only had the "your school" search bar.
- **L6** — the `l6-sponsored-ads.png` label coordinates are hand-set approximations (Sponsored ~88/28, Ad ~24/80,
  search word ~22/6); fine for self-marked, but worth a click-to-capture pass if the picker lands.

## Type gaps
None new. All demand met by existing types (label / sort / order / multi-select / single-choice / screenshot).

## Self-verify
PASS — 6 lessons. Each: slides title ends `.md`; ≥4 slides (7–8) + non-empty teacher notes; activity has a 📷
screenshot field; Support≠Challenge slices differ; every `{{res:}}` resolves to a manifest file on disk.
