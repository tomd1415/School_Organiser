# 2D lists — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use a **2D list** (a list of lists) and get items with **two indexes**: `list[row][column]`.

## Starter — predict this program
A **2D list** holds lists inside a list. You pick a row, then an item in that row.

```python
scores = [["Fred", "Wilma", "Dino"],
          [4, 5, 6]]
print(scores[1][2])
```

| Question | Your prediction |
|---|---|
| `scores[1]` is the second row `[4, 5, 6]`. What is `scores[1][2]`? | Type your answer here |

## 🟢 Support
| Question | Choose |
|---|---|
| A 2D list is… | (  ) a list of lists (  ) a single list (  ) a string |
| `scores[1]` gives… | (  ) the whole second row (  ) one number (  ) the first row |

## 🟡 Core
| Question | Your answer |
|---|---|
| Write exactly what `scores[1][2]` prints. | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| In `list[row][column]`, which index comes first — the row or the column? | Type your answer here |
