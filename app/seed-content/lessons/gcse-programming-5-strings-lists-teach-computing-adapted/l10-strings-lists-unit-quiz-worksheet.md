# Strings and lists — unit quiz

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
This quiz checks what I have learned across the whole unit: strings, slicing, lists, list methods and 2D lists.

## Key words — match each to its meaning
| Word | What it means |
|---|---|
| Substring | (  ) part of a string (  ) joining strings together (  ) a list of lists |
| Concatenate | (  ) part of a string (  ) joining strings together (  ) a list of lists |
| 2D list | (  ) part of a string (  ) joining strings together (  ) a list of lists |

## 🟢 Support
| Question | Choose |
|---|---|
| `word[0]` gives you… | (  ) the first character (  ) the whole word (  ) the length |
| `len(word)` gives you… | (  ) how many characters (  ) the first letter (  ) a list |
| A list is… | (  ) dynamic (can change) (  ) static (fixed) |

## 🟡 Core
For `word = "PYTHON"` (P=0, Y=1, T=2, H=3, O=4, N=5):

| Question | Your answer |
|---|---|
| `word[0:3]` gives the substring [[ ]] | |
| `word[2]` gives the character [[ ]] | |
| For `animals = [["Salmon","Pollock"],["Parrot","Duck"]]`, `animals[1][0]` gives [[ ]] | |

Tick **all** that are list **methods**:

| Question | Tick all that apply |
|---|---|
| Which of these are list methods? | [  ] append [  ] sort [  ] reverse [  ] len |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write a program that asks for a word and prints how many letters it has and its first letter. | Type your code here |
| Explain the difference between a 1D list and a 2D list, with an example of each. | Type your answer here |

## How confident are you with this unit?
| Question | Your answer |
|---|---|
| Rate your confidence with strings and lists | [scale 1-5: not yet … very confident] |

## ✅ I can…
- [ ] I can use string indexing and slicing
- [ ] I can use list methods
- [ ] I can access items in a 2D list
- [ ] I can write a short program using strings and lists
