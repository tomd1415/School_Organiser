# Classes and objects — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
A **class** is a blueprint. An **object** is a thing made from that blueprint.
A class holds **attributes** (data, like `name`) and **methods** (actions, like `get_name`).
A **constructor** builds a new object.

![A cartoon cat — our example object]({{res:l2-cat-object.png}})

## Match the words
Drag each word to its meaning.

| Word | What it means |
|---|---|
| Class | (  ) the blueprint for an object (  ) a thing made from the blueprint (  ) a piece of data an object has (  ) an action an object can do |
| Object | (  ) the blueprint for an object (  ) a thing made from the blueprint (  ) a piece of data an object has (  ) an action an object can do |
| Attribute | (  ) the blueprint for an object (  ) a thing made from the blueprint (  ) a piece of data an object has (  ) an action an object can do |
| Method | (  ) the blueprint for an object (  ) a thing made from the blueprint (  ) a piece of data an object has (  ) an action an object can do |

## Attribute or method?
Here are the parts of the `Pet` class. Drag each one into the right group.

```sort
Attributes: name, species, description
Methods: get_name, set_name, describe
```

## Build the constructor
The constructor stores each value on the object with `self`. Fill in the gaps.

The line that stores the name is `self.name = [[ ]]`.
The line that stores the species is `self.species = [[ ]]`.

## 🟢 Support
| Question | Choose |
|---|---|
| An attribute is a piece of… | (  ) data an object has (  ) cake (  ) cable |
| A method is an… | (  ) action an object can do (  ) error (  ) image |

> Tip: attribute = a noun (a thing). Method = a verb (an action).

## 🟡 Core
| Question | Your answer |
|---|---|
| What does a constructor do? | Type your answer here |
| A getter method like `get_name` does what? | Type your answer here |

## 🔴 Challenge
Drag the lines into the right order to make a working getter method.

```parsons
def get_name(self):
    return self.name
```

| Question | Your answer |
|---|---|
| Why do we use a getter method instead of reading the attribute straight from outside the class? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste your code link (repl.it or similar) here | Type your answer here |
| Show your Pet object running | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I can say how a class and an object are related
- [ ] I can tell an attribute from a method
- [ ] I can use a constructor to make an object
- [ ] I can get and set an attribute on an object
