# Crunching numbers — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use arithmetic in Python, and turn keyboard input into a number with `int()`.

## Predict
Here is a worked example: a program that works out your weight on the moon.

![A Python program that works out weight on the moon]({{res:l2-weight-on-the-moon-code-teach-computing.png}})

| Question | Your prediction |
|---|---|
| If the user types 60, the program prints `Weight on moon: 10.0`. Where does the 10 come from? | Type your answer here |

## Order matters
This program is meant to print `2567 seconds is 42 whole minutes`, but the lines are jumbled. A variable must be given a value **before** it is used. Drag the lines into the right order.

```parsons
secs = 2567
mins = secs // 60
print(secs, "seconds is", mins, "whole minutes")
```

## 🟢 Support
Keyboard input always arrives as text. To do maths with it, wrap it in `int()`.

| Question | Your answer |
|---|---|
| Fill the gap so the typed number becomes a whole number: weight_earth = [[ ]](input()) | Type your answer here |

## 🟡 Core
| Question | Your answer |
|---|---|
| Write a program that asks for a weight on Earth and prints one sixth of it (the weight on the moon). | Type your code here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write a "dog years" program: ask the user's age and print it multiplied by 7. | Type your code here |
| Why do we need `int()` around `input()` before doing the multiplication? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste a link to your saved program here | Type your answer here |
| Show your finished program | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I used arithmetic operators to calculate a value
- [ ] I put jumbled lines in the right order so a variable is set before it is used
- [ ] I used `int()` to turn keyboard input into a number
- [ ] I wrote a program that does a calculation
