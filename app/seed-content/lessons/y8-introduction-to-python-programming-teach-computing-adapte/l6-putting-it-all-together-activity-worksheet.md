# Putting it all together — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will combine a loop and selection, and use a Boolean flag, to finish a number guessing game.

## Predict
Here is the start of the guessing game. A flag called `guessed` keeps track of whether the user has won.

![A Python guessing game that uses a Boolean flag]({{res:l6-lucky-number-flag-code-teach-computing.png}})

| Question | Your prediction |
|---|---|
| `guessed` starts as `False` and is never set to `True`. What will the game keep doing? | Type your answer here |

## Words for true and false
Match each word to what it means.

| Word | What it means |
|---|---|
| True | (  ) yes / on (  ) no / off (  ) a True/False variable used as a marker (  ) true only when both parts are true |
| False | (  ) yes / on (  ) no / off (  ) a True/False variable used as a marker (  ) true only when both parts are true |
| flag | (  ) yes / on (  ) no / off (  ) a True/False variable used as a marker (  ) true only when both parts are true |
| and | (  ) yes / on (  ) no / off (  ) a True/False variable used as a marker (  ) true only when both parts are true |

## 🟢 Support
| Question | Your answer |
|---|---|
| Fill the gap to raise the flag when the user wins: guessed = [[ ]] | Type your answer here |
| Fill the gap so the loop runs while the user has NOT won: while guessed == [[ ]]: | Type your answer here |

## 🟡 Core
| Question | Your answer |
|---|---|
| Finish the game: set the flag to `True` when the guess is right, and count how many guesses the user takes. | Type your code here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Limit the user to 3 guesses using `and` in the while condition, and reveal the lucky number if they run out. | Type your code here |

## Show your work
| Question | Your answer |
|---|---|
| Paste a link to your finished game here | Type your answer here |
| Show your finished game running | 📷 Paste a screenshot of your work here |

## Reflection
| Question | Your answer |
|---|---|
| Write one thing you learnt in this Python unit. | Type your answer here |
| How confident are you writing a Python program now? | [scale 1-5: not sure … very confident] |

## ✅ I can…
- [ ] I combined a `while` loop with `if`/`else`
- [ ] I used a Boolean flag to control a loop
- [ ] I counted and limited the number of guesses
- [ ] I finished and tested a complete guessing game
