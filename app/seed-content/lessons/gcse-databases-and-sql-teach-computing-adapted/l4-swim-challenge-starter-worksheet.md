# Swim database — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
First I will remember how tables are linked by primary and foreign keys.

## The swim database
A leisure centre uses three linked tables to manage swimming lessons.

![The three linked tables of the swim database]({{res:l4-swim-database-schema-teach-computing.png}})

## Sort the keys
The new table tblLessons has three fields. Drag each field into the kind of key it is.

```sort
Primary key: LessonID
Foreign key: CourseID, MemberID
```

## 🟢 Support
| Question | Choose |
|---|---|
| A primary key must be… | (  ) different for every record (  ) the same for every record (  ) always empty |
| A foreign key is used to… | (  ) link to another table (  ) delete a table (  ) sort a table |

## 🟡 Core
| Question | Your answer |
|---|---|
| One course has many lessons. Explain what a "one-to-many" relationship means here. | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| A foreign key value can appear many times, but a primary key value cannot repeat. Explain why. | Type your answer here |
