# Insert, update, delete — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
First I will choose the right data type for each field in a table.

## Starter — sort the fields by data type
A doctor's surgery stores patient data. Drag each field into the SQLite data type that fits it best.

```sort
INTEGER: PatientID, AppointmentID
TEXT: Firstname, Surname, Date_of_birth
REAL: Height in cm
```

## 🟢 Support
| Question | Choose |
|---|---|
| An INTEGER stores… | (  ) a whole number (  ) some text (  ) a decimal number |
| TEXT stores… | (  ) letters and words (  ) only whole numbers (  ) nothing |

## 🟡 Core
Match each SQLite data type to what it stores.

| Data type | Stores |
|---|---|
| INTEGER | (  ) a whole number (  ) text (  ) a decimal number |
| TEXT | (  ) a whole number (  ) text (  ) a decimal number |
| REAL | (  ) a whole number (  ) text (  ) a decimal number |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| In SQLite a date of birth is stored as TEXT. Why is TEXT used and not a plain number? | Type your answer here |
