# Database essentials — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will name the parts of a database: table, record, field and primary key.

## Starter — how is data stored?
The DVLA keeps data about every vehicle in the UK. Look at what it shows about one car.

![DVLA vehicle details]({{res:l1-dvla-vehicle-details-teach-computing.png}})

| Question | Choose |
|---|---|
| How is the DVLA storing all this vehicle data? | (  ) in a database (  ) on one piece of paper (  ) in a photo |

## Label the parts of a table
Drag the word onto the right spot on the music table. A = the whole grid, B = the columns, C = one row.

```label
image: {{res:l1-music-table-key-terms-teach-computing.png}}
A (8%, 54%): Table
B (66%, 41%): Field
C (93%, 92%): Record
```

## 🟢 Support
Match each word to what it means. Drag the word onto its meaning.

| Meaning | Match the word |
|---|---|
| A column of one kind of data, like Artist | (  ) Field (  ) Record (  ) Table (  ) Primary key |
| One row — all the data about one track | (  ) Field (  ) Record (  ) Table (  ) Primary key |
| The whole grid of rows and columns | (  ) Field (  ) Record (  ) Table (  ) Primary key |
| A field with a different value for every row | (  ) Field (  ) Record (  ) Table (  ) Primary key |

## 🟡 Core
| Question | Your answer |
|---|---|
| What is a primary key? | Type your answer here |

## 🔴 Challenge
| Question | Tick ALL that are true |
|---|---|
| Select ALL the properties of a primary key. | [ ] Each value is unique [ ] It identifies each record [ ] The same value can be used many times [ ] It must always be a number |
