# Sense HAT II — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use **choice()** to pick a random item from a list, and **append** random items to build a list.

## Starter — predict this program
`choice()` picks one random item from a list.

```python
from random import choice
colours = ["red", "yellow", "blue"]
colour = choice(colours)
print(colour)
```

| Question | Your prediction |
|---|---|
| What kind of thing will `colour` hold each time you run it? | Type your answer here |

## 🟢 Support
| Question | Choose |
|---|---|
| `choice(colours)` picks… | (  ) one random item from the list (  ) the whole list (  ) the first item |
| If you run it twice, the result… | (  ) might be different each time (  ) is always the same |

## 🟡 Core
| Question | Your answer |
|---|---|
| Name one of the three things `colour` could hold. | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Last lesson you used `randint()` to pick a random index. How is `choice()` simpler for picking from a list? | Type your answer here |
