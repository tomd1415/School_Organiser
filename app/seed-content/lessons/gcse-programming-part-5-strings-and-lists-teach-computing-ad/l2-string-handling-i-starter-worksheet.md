# String handling I — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will learn how to find the **length** of a string, get a **character** by its **index**, and loop over a string.

## Starter — predict this program
A **string** is text. Read this before you run it.

```python
word = "sheep"
print(len(word))
print(word[0])
```

| Question | Your prediction |
|---|---|
| What do you think the two `print` lines will output? | Type your answer here |

## 🟢 Support
| Question | Choose |
|---|---|
| `len(word)` gives you… | (  ) how many characters are in the string (  ) the first letter (  ) the last letter |
| `word[0]` gives you… | (  ) the first character (  ) the whole word (  ) the length |

## 🟡 Core
| Question | Your answer |
|---|---|
| Write exactly what `print(len(word))` and `print(word[0])` output for `word = "sheep"`. | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Indexing starts at 0. Which index would you use to get the letter `e` in `"sheep"` (the first one)? | Type your answer here |
