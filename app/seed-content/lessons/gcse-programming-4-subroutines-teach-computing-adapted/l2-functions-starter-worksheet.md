# Functions — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will learn what **return** does, and use a **trace table** to follow a function.

## Starter — predict this program
Read this program before you run it. Imagine `num1` is **2** and `num2` is **3**.

```python
def powers(a, b):
    answer = a ** b

print("Enter a number:")
num1 = int(input())
print("Enter a second number:")
num2 = int(input())
powers(num1, num2)
print(f"{num1} to the power of {num2} is {answer}")
```

| Question | Your prediction |
|---|---|
| What do you think this program will do when it runs? | Type your answer here |

## 🟢 Support
| Question | Choose |
|---|---|
| `answer` is made INSIDE the subroutine. Can the last line use it? | (  ) no — it only exists inside (  ) yes — always (  ) only on Mondays |
| To send a value back OUT of a function, you use the word… | (  ) return (  ) print (  ) input |

## 🟡 Core
| Question | Your answer |
|---|---|
| The program crashes with "answer is not defined". Explain why in one sentence. | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| How would adding `return answer` to the function, and `answer = powers(num1, num2)`, fix the program? | Type your answer here |
