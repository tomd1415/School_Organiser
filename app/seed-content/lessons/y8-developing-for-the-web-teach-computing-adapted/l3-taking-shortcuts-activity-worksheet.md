# Taking shortcuts — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use CSS to style a web page, and explain why CSS beats styling by hand.

## What is CSS?
CSS (Cascading Style Sheets) keeps all the styling in one separate file. Change it once, and every matching tag updates.

![A web page styled with CSS]({{res:l3-web-page-styled.png}})

| Question | Your prediction |
|---|---|
| A CSS rule says every `<h2>` is green. The page has five `<h2>` headings. How many turn green? | Type your answer here |

## Match the CSS property to what it changes
Drag each answer to the right property.

| CSS property | What it changes |
|---|---|
| `color` | (  ) text colour (  ) text size (  ) a line around an element (  ) the page background |
| `font-size` | (  ) text colour (  ) text size (  ) a line around an element (  ) the page background |
| `border` | (  ) text colour (  ) text size (  ) a line around an element (  ) the page background |
| `background` | (  ) text colour (  ) text size (  ) a line around an element (  ) the page background |

## 🟢 Support
Sort where each kind of styling lives.

```sort
Inline style: written inside one tag, changes only that one tag
CSS file: kept in a separate file, changes every matching tag at once
```

## 🟡 Core
Put the steps to set up CSS in the correct order.

```order
Open the CSS file in a plain text editor
Write the tag name you want to style, then { }
Put the style rules inside the curly brackets
Save the CSS file
Open the HTML page in a browser to see the changes
```

| Question | Your answer |
|---|---|
| Why is a CSS file better when the same tag is used many times? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write a CSS rule that gives every `<p>` paragraph a blue underline. | Type your code here |
| You need to change every heading from red to green. With CSS, how many lines do you edit? Explain. | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste the CSS you wrote here | Type your code here |
| Show your styled page in the browser | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what CSS is
- [ ] I used CSS to style a web page
- [ ] I explained why CSS is better than styling by hand
