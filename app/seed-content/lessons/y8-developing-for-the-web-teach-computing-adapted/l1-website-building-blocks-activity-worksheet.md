# Website building blocks — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use HTML tags to build a web page, then style a tag to change how it looks.

## Predict
Look at the code on the board before it opens in the browser.

| Question | Your prediction |
|---|---|
| What will `<h1>My pets</h1>` look like on the page? | Type your answer here |

## Match the tag to what it does
Drag each answer to the right tag.

| Tag | What it does |
|---|---|
| `<h1>` | (  ) a big heading (  ) bold text (  ) italic text (  ) a paragraph |
| `<b>` | (  ) a big heading (  ) bold text (  ) italic text (  ) a paragraph |
| `<em>` | (  ) a big heading (  ) bold text (  ) italic text (  ) a paragraph |
| `<p>` | (  ) a big heading (  ) bold text (  ) italic text (  ) a paragraph |

## 🟢 Support
Here is a plain page, then the same page after a tag was styled.

![A plain web page with black default text]({{res:l1-plain-web-page-teach-computing.png}})

![The same page with a red, centred heading and a background colour]({{res:l1-styled-web-page-teach-computing.png}})

| Question | Choose |
|---|---|
| An HTML tag is written inside which brackets? | (  ) < > (  ) ( ) (  ) { } |
| A closing tag is different from an opening tag because it has a… | (  ) / slash (  ) * star (  ) ! mark |

## 🟡 Core
Put the HTML lines in the correct order to make a valid web page.

```parsons
<!DOCTYPE html>
<html>
<body>
<h1>My pets</h1>
<p>I have a dog.</p>
</body>
</html>
```

| Question | Your answer |
|---|---|
| What does the style modifier `color:red;` do to a heading? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write the inline style that makes an `<h1>` heading red AND centred. | Type your answer here |
| Why would styling every single tag by hand become a problem on a big web page? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste the HTML you wrote here | Type your code here |
| Show your styled web page in the browser | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what HTML is
- [ ] I used HTML tags to structure a web page
- [ ] I modified a tag with inline styling to change how it looks
