# Searching the web — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will explain how a search engine crawls and indexes the web, and what makes a page rank higher.

## Crawlers follow links
A web crawler (also called a spider) visits a page, records what it finds, then follows the links to the next page.

![A web crawler following links from page to page]({{res:l4-web-crawler-following-links-teach-computing.png}})

| Question | Your prediction |
|---|---|
| How does a crawler get from one web page to the next? | Type your answer here |

## Match the word to its meaning
Drag each answer to the right word.

| Word | What it means |
|---|---|
| Crawler | (  ) a program that visits and records web pages (  ) a stored list of pages and keywords (  ) the words you type to search (  ) the order results are shown in |
| Index | (  ) a program that visits and records web pages (  ) a stored list of pages and keywords (  ) the words you type to search (  ) the order results are shown in |
| Query | (  ) a program that visits and records web pages (  ) a stored list of pages and keywords (  ) the words you type to search (  ) the order results are shown in |
| Ranking | (  ) a program that visits and records web pages (  ) a stored list of pages and keywords (  ) the words you type to search (  ) the order results are shown in |

## 🟢 Support
Sort what a crawler does and does not record.

```sort
Recorded: keywords, metatags, hyperlinks, the date updated
Ignored: small words like the, common words like and
```

## 🟡 Core
Put the steps a crawler takes in the correct order.

```order
Visit a web page
Read the metatags in the head
Record the keywords and how often they appear
Record the hyperlinks on the page
Store it all in the search engine index
Follow a link to the next page
```

Now act like a crawler. Read this page's code.

![An example web page the crawler is reading]({{res:l4-example-web-page-teach-computing.png}})

| Question | Your answer |
|---|---|
| Write THREE keywords a crawler would record for a page titled "Scratch (programming language)". | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Two pages have the same keywords. Give one reason a search engine ranks one ABOVE the other. | Type your answer here |
| Why does a page that is updated often tend to rank higher than an old one? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste the HTML for your well-designed page here | Type your code here |
| Show your page in the browser | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what a search engine is
- [ ] I explained how a crawler finds and indexes pages
- [ ] I described what makes a page rank higher
