# Words are not enough — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## Starter — tags recap
Tags are the building blocks of every web page. Here are some you have already met.

![A list of common HTML tags and what they do]({{res:l2-html-tags-reference.png}})

Complete the missing **closing** tags:

- A heading opens with `<h1>` and closes with [[ ]].
- A paragraph opens with `<p>` and closes with [[ ]].
- Bold text opens with `<b>` and closes with [[ ]].

## 🟢 Support
| Question | Choose |
|---|---|
| A closing tag always starts with… | (  ) </ (  ) <* (  ) <! |

## 🟡 Core
| Question | Your answer |
|---|---|
| `<em>` and `<strong>` also help people who use a screen reader. Why is that a good thing? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| `<em>` and `<i>` both make text italic. Give one reason a web designer might choose `<em>`. | Type your answer here |
