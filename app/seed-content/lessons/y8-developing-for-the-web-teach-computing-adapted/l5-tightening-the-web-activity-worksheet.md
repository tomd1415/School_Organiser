# Tightening the web — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use search operators to control my results, and create a hyperlink between two web pages.

## Match the operator to what it does
Drag each answer to the right operator.

| Search operator | What it does |
|---|---|
| `OR` | (  ) finds pages with either word (  ) removes pages with that word (  ) finds the exact phrase |
| `NOT (-)` | (  ) finds pages with either word (  ) removes pages with that word (  ) finds the exact phrase |
| `"quotes"` | (  ) finds pages with either word (  ) removes pages with that word (  ) finds the exact phrase |

## 🟢 Support
Sort each operator by what it does to the number of results.

```sort
Broadens (more results): OR
Narrows (fewer results): NOT (-), quote search
```

## 🟡 Core
| Question | Choose |
|---|---|
| Which search is likely to return the MOST results? | (  ) Turing OR Babbage (  ) "Turing Babbage" (  ) Turing -Babbage |

| Question | Your answer |
|---|---|
| You want restaurants called Taj Mahal in the USA but NOT in New York. Which operator removes New York? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write a search that finds pages about Alan Turing OR Charles Babbage, using only those two surnames. | Type your answer here |

Put these lines in order to make a working hyperlink to the `css.html` page.

```parsons
<a href="css.html">
CSS page
</a>
```

## Show your work
| Question | Your answer |
|---|---|
| Paste the HTML for your new page (with its hyperlink) here | Type your code here |
| Show your linked pages working in the browser | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I used search operators to control my results
- [ ] I explained how an operator changes the number of results
- [ ] I created a hyperlink between two web pages
