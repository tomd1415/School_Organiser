# Navigating the web — end-of-unit quiz worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## End-of-unit quiz
This quiz checks what you have learned across the whole unit. Pick ONE answer for each question.

| Question | Choose |
|---|---|
| 1. Which tag makes the main heading on a page? | (  ) `<h1>` (  ) `<h2>` (  ) `<b>` (  ) `<u>` |
| 2. Which style is spelled wrongly? | (  ) `color:red;` (  ) `font-family:courier;` (  ) `colour:blue;` (  ) `text-align:center;` |
| 3. What is the `alt` attribute on an `img` tag for? | (  ) a text description of the image (  ) makes the image bigger (  ) changes the image colour (  ) deletes the image |
| 4. What does `text-align:center;` do? | (  ) centres the content (  ) makes it bold (  ) changes the colour (  ) adds a border |
| 5. What is a benefit of CSS? | (  ) all the styling of a tag is set in one file (  ) the page loads instantly (  ) it finds your page on Google (  ) it adds images for you |
| 6. What does `body { color: green; }` do? | (  ) makes the page text green (  ) makes the background green (  ) makes the border green (  ) makes the title green |
| 7. What are programs that find web pages for search engines called? | (  ) crawlers (  ) webbers (  ) searchers (  ) indexers |
| 8. Which of these does NOT affect ranking? | (  ) words that are not on the page (  ) when the page was last updated (  ) pages that link to it (  ) keywords near the top |
| 9. Which search gives the MOST results? | (  ) cats OR dogs (  ) "cats dogs" (  ) cats -dogs (  ) cats dogs |
| 10. What is wrong with `<a href="quiz.htm">Quiz<a>`? | (  ) the tag is not closed (  ) it should say link not href (  ) text cannot go in the tag (  ) the quotes are wrong |

## ✅ I can…
- [ ] I answered the quiz on my own
- [ ] I checked each answer before moving on
