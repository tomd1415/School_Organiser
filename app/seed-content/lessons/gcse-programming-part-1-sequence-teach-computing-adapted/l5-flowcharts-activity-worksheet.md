# Flowcharts — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
A **flowchart** uses shapes to plan a program. Today I will follow a flowchart and design my own.

This flowchart uses the main shapes — the rounded **terminators** (Start/End), a **subroutine** box (welcome) and an input/output **parallelogram**:

![A worked flowchart with the code beside it]({{res:l5-a-worked-flowchart-with-code-teach-computing.png}})

## Pair programming
Work in a pair. One person is the **driver** (types). One is the **navigator** (reads and checks). Swap every 5 minutes.

![Two pupils pair programming — a driver and a navigator]({{res:l5-pair-programming-teach-computing.png}})

## 🟢 Support
Match each flowchart symbol to what it is used for.

| Flowchart symbol | Match |
|---|---|
| Rounded box at the very start or end | (  ) terminator (  ) input/output (  ) process (  ) subroutine |
| Parallelogram for getting or showing data | (  ) terminator (  ) input/output (  ) process (  ) subroutine |
| Rectangle for a calculation or step | (  ) terminator (  ) input/output (  ) process (  ) subroutine |
| Rectangle with side bars that calls named code | (  ) terminator (  ) input/output (  ) process (  ) subroutine |

## 🟡 Core
The "always 3" mind-reading trick. Drag the steps into the right order.

```order
Pick a number between 1 and 10
Multiply the number by 2
Multiply the new answer by 5
Divide the answer by the number you first picked
Subtract 7 from the answer
The answer is always 3
```

## 🔴 Challenge
The "Susan from Space" chatbot flowchart asks the user's name, then says hello.

| Question | Your answer |
|---|---|
| Write the Python for those first two steps (ask the name, then greet the user). | Type your code here |

## Show your work
| Question | Your answer |
|---|---|
| Paste your Trinket share link here | Type your answer here |
| Show your finished flowchart | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I named the main flowchart symbols
- [ ] I followed a flowchart and wrote the code it shows
- [ ] I put the steps of a program in the right order
- [ ] I designed a simple flowchart
