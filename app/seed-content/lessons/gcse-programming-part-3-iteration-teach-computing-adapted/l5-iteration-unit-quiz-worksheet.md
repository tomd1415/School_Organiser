# Iteration unit quiz

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
This quiz checks everything from the iteration unit: **while loops, trace tables, for loops, validation, and pseudocode**.

## Key words — match each to its meaning
| Term | Meaning |
|---|---|
| Iteration | (  ) repeating a group of instructions (  ) a value with a name (  ) checking input is sensible (  ) informal steps in English |
| Variable | (  ) repeating a group of instructions (  ) a value with a name (  ) checking input is sensible (  ) informal steps in English |
| Data validation | (  ) repeating a group of instructions (  ) a value with a name (  ) checking input is sensible (  ) informal steps in English |
| Pseudocode | (  ) repeating a group of instructions (  ) a value with a name (  ) checking input is sensible (  ) informal steps in English |

## Sort the loops
```sort
While loop: condition-controlled, repeats until the condition is False, indefinite
For loop: count-controlled, repeats a set number of times, uses range()
```

## 🟢 Support
| Question | Choose |
|---|---|
| A while loop keeps repeating while its condition is… | (  ) True (  ) False (  ) empty |
| `range(1, 11)` makes the loop count… | (  ) 1 to 10 (  ) 1 to 11 (  ) 0 to 10 |
| A tool that walks through code line by line to find errors is a… | (  ) trace table (  ) for loop (  ) variable |

## 🟡 Core
Tick **all** the statements that are true about a **for loop**.

| Question | Tick all that are true |
|---|---|
| Which of these are true about a for loop? | [ ] it repeats a set number of times [ ] it uses range() to count [ ] it runs forever [ ] it is definite iteration |

Fill in the gaps:

A for loop using `range(1, 6)` counts from 1 to [[ ]].

A while loop stops when its condition becomes [[ ]].

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write a for loop that prints the numbers 1 to 5, each on its own line. Type your program. | Type your code here |
| Write a while loop that keeps asking "Guess the word" until the user types "Python". Type your program. | Type your code here |

## How confident do you feel?
| Question | Your answer |
|---|---|
| How confident are you with loops now? | [scale 1-5: not sure … very confident] |

## Show your work
| Question | Your answer |
|---|---|
| Show your finished quiz code | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I know what iteration means
- [ ] I can tell a while loop from a for loop
- [ ] I can use a trace table to find an error
- [ ] I can explain why we validate input
- [ ] I can read pseudocode and turn it into Python
