# You've got the moves! — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## Starter — custom blocks are subroutines
A **subroutine** is a group of instructions with a name. It runs when the main program **calls** it.
Look at this Scratch code. The pink blocks `define twist` and `define jump_spin` are two subroutines.

![Scratch code: two subroutines (define twist, define jump_spin) and a key press that calls them]({{res:l1-dance-battle-subroutines-teach-computing.png}})

| Question | Tick the best answer |
|---|---|
| A subroutine is… | (  ) a group of instructions with a name that you can call (  ) a single costume (  ) a sound effect |

## 🟢 Support
| Question | Tick the best answer |
|---|---|
| When the `a` key is pressed, the sprite will… | (  ) run the `twist` subroutine (  ) stop the program (  ) change colour |

## 🟡 Core
| Question | Your answer |
|---|---|
| In your own words, what does the `twist` block do when it is called? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Why is it useful to give each dance move its own subroutine? | Type your answer here |
