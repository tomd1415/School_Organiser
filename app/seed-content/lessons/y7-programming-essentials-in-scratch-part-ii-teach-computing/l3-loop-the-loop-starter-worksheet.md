# Loop the loop! — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## Starter — describe these loops
Scratch gives you three kinds of loop. Here is a count-controlled one — it draws a square.

![Scratch code: when green flag clicked, pen down, repeat 4 (move 200 steps, turn 90 degrees)]({{res:l3-count-controlled-repeat-loop-square-teach-computing.png}})

## 🟢 Support
Match each loop to what it does. Pick one answer on every row.

| Loop | What it does |
|---|---|
| `forever` | (  ) runs the code over and over until the game is stopped (  ) runs the code a set number of times (  ) runs the code until a condition becomes true |
| `repeat (4)` | (  ) runs the code over and over until the game is stopped (  ) runs the code a set number of times (  ) runs the code until a condition becomes true |
| `repeat until` | (  ) runs the code over and over until the game is stopped (  ) runs the code a set number of times (  ) runs the code until a condition becomes true |

## 🟡 Core
| Question | Tick the best answer |
|---|---|
| The `repeat (4)` loop above is… | (  ) count-controlled (  ) condition-controlled |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Explain the difference between a count-controlled loop and a condition-controlled loop. | Type your answer here |
