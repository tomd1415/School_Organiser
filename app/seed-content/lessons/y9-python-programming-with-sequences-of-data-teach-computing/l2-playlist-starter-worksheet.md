# Playlist — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## Starter — find the item
This list of planets is numbered from 0.

`planets = ["Mercury", "Venus", "Earth", "Mars", "Jupiter"]`

| Question | Choose |
|---|---|
| What does `planets[2]` give? | (  ) Earth (  ) Venus (  ) Mars |

## 🟢 Support
| Question | Choose |
|---|---|
| The first item in any list has the index… | (  ) 0 (  ) 1 (  ) -1 |

## 🟡 Core
Match each list operation to what it does. Drag each answer to its operation.

| Operation | What it does |
|---|---|
| `append` | (  ) add an item to the end (  ) remove an item (  ) find an item's index (  ) count how often an item appears |
| `remove` | (  ) add an item to the end (  ) remove an item (  ) find an item's index (  ) count how often an item appears |
| `index` | (  ) add an item to the end (  ) remove an item (  ) find an item's index (  ) count how often an item appears |
| `count` | (  ) add an item to the end (  ) remove an item (  ) find an item's index (  ) count how often an item appears |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| `pop()` and `remove()` both take an item out of a list. What is the difference between them? | Type your answer here |
