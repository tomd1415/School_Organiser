# Warm up — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will recap selection (`if` / `elif` / `else`), fix common syntax errors, and start using lists.

## Predict — how long until the weekend?
Look at this program before it runs. Day numbers go from 0 (Monday) to 6 (Sunday).

![A Python program that uses if-else to check the day of the week]({{res:l1-weekday-selection-code-teach-computing.png}})

| Question | Choose |
|---|---|
| The user types 2 (Wednesday). What does the program display? | (  ) It's a weekday (  ) It's the weekend! (  ) Nothing |

## Spot the syntax errors  (we do)
Some lines have a syntax error and some are fine. Drag each line into the right group.

```sort
Has a syntax error: if day = 4:, print("Monday, if day <= 4
No syntax error: day = int(input()), if day <= 4:
```

## 🟢 Support — read an item from a list
Lists are numbered starting at 0. Look at the `months` list.

![A Python list of month names, accessed by index]({{res:l1-months-list-code-teach-computing.png}})

| Question | Choose |
|---|---|
| Which index gives `"March"`? | (  ) 2 (  ) 3 (  ) 1 |

## 🟡 Core
| Question | Your answer |
|---|---|
| `"June"` is item number [[ ]] in the months list (counting starts at 0). | Type your answer here |
| In your own words, what does an index do? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| The line `remaining = ` is empty. Write an expression for the days left until the weekend (Saturday is day 5). | Type your code here |
| `if day = 4:` does not work. What is the error, and what should the line be? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste a link to your saved program here | Type your answer here |
| Show your finished program | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I predicted what a selection program displays
- [ ] I found and fixed common syntax errors
- [ ] I created a list and read an item using its index
- [ ] I used selection to choose what my program does
