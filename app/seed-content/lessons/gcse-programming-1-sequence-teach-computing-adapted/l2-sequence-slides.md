# Sequence — your first Python code

## Today we are learning
- the tools an IDE gives a programmer
- to run a Python program made of subroutines
- to predict what a sequence will do
- to spot syntax and logic errors

> 🧑‍🏫 Read the four "I can…" aloud. Vocabulary on board: IDE · subroutine · sequence · syntax error · logic error. Everyone has the Python IDE (Mu / Trinket) open.

## Starter — why do we need translators?
Quick recap from last lesson.

> 🧑‍🏫 Think · write · pair · share on the starter worksheet. Answer: "Computers can only run machine code, so translators are needed for high-level programs."

## What is an IDE?
An IDE helps you: colour the code, check the syntax, indent for you, autocomplete, and find errors.

> 🧑‍🏫 Show the IDE. Point out each helpful feature as you type. An IDE is much friendlier than a plain text editor.

## I do — our first program
![The welcome() program]({{res:l2-first-python-code-teach-computing.png}})

A subroutine is named code you can call. `welcome()` calls it.

> 🧑‍🏫 I-do live coding. Make a syntax error on purpose (delete a colon) — show the message. Then a logic error (do not call welcome()) — it runs but nothing happens.

## Watch — live coding example
*(teacher demo video — play it live; not embedded, to keep the repo light)*

> 🧑‍🏫 Teacher-played hook (has motion + sound). Optional — play it, then code it live yourself. Not for pupils to play alone.

## We do — predict the Twinkle program
Read the code. Predict what it shows BEFORE you run it.

> 🧑‍🏫 We-do PRIMM. Stress: only twinkle() is called, so only its lines run. That is why the rhyme stops.

## Your turn — run, investigate, modify
Use the activity worksheet. Sort the errors, answer the questions, then put the code in order.

> 🧑‍🏫 You-do. Support: error card-sort. Challenge: Parsons order the subroutine calls. Likely error: forgetting to call a subroutine → "a subroutine only runs when it is CALLED."

## I can…
Tick your four "I can…". Show me your program running.

> 🧑‍🏫 Plenary: "What is the difference between a syntax error and a logic error?" Note who met all four.
