# Input — adding interactivity

## Today we are learning
- to get input from the keyboard with input()
- the five data types: integer, real, Boolean, character, string
- to change a data type with int() or float() (casting)
- what a runtime error is

> 🧑‍🏫 Read the four "I can…" aloud. Vocabulary on board: input · integer · real · Boolean · character · string · casting · runtime error.

## Starter — variables recap
Three multiple-choice questions about last lesson.

> 🧑‍🏫 Use the starter worksheet. These check the misconceptions: print syntax, a variable must be set before use, a variable holds one value at a time.

## I do — the input() function
`name = input()` waits for the user to type, then stores what they typed.

> 🧑‍🏫 I-do live coding. Show input() pausing for the user. Then ask: "how could inputs improve our silly stories?"

## Watch — live coding example
*(teacher demo video — play it live; not embedded, to keep the repo light)*

> 🧑‍🏫 Teacher-played hook (motion + sound). For teacher use — preview before the lesson. Optional to show pupils.

## We do — the five data types
![The data collection program]({{res:l4-data-collection-code-teach-computing.png}})

integer (7) · real (3.14) · string ("hi") · Boolean (True/False) · character ("a").

> 🧑‍🏫 We-do. input() ALWAYS gives a string. Use int() or float() to cast it to a number. Likely error: typing letters for a number → a runtime error (ValueError).

## Your turn — predict, run, investigate
Use the activity worksheet: sort the data types, then investigate the program.

> 🧑‍🏫 You-do PRIMM. Support: data-type card-sort. Challenge: explain the runtime error and validation. Circulate.

## I can…
Tick your four "I can…". Name the five data types to your partner.

> 🧑‍🏫 Plenary: quick recall of the five data types. Note who met all four.
