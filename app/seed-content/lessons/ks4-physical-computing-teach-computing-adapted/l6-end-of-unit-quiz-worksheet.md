# End-of-unit quiz — physical computing

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
This quiz checks what I have learned across the whole physical computing project. Answer in exam conditions and do your best.

## The buggy you built
![The finished buggy with its brake lights on]({{res:l6-buggy-brakelights.png}})

## Key words
Match each key word to its meaning.

| Key word | What it means |
|---|---|
| microcontroller | (  ) a tiny computer on one chip (  ) a board that drives motors (  ) a sensor that measures distance (  ) pins for inputs and outputs |
| embedded system | (  ) a computer built inside a product to do one job (  ) the internet (  ) a screen (  ) a battery |
| GPIO pins | (  ) pins for connecting inputs and outputs (  ) a tiny computer on one chip (  ) a board that drives motors (  ) a sensor that measures distance |
| motor controller | (  ) a board that drives the motors (  ) a tiny computer on one chip (  ) the internet (  ) a screen |

## Inputs and outputs
| Question | Tick ALL that are OUTPUT devices |
|---|---|
| Which of these are outputs? | [ ] an LED [ ] a DC motor [ ] a button [ ] an ultrasonic sensor |

## Code and pins
A speed-camera robot uses the same ideas as your buggy.

| Question | Choose |
|---|---|
| To set GP15 as an output for an LED you write… | (  ) Pin(15, Pin.OUT) (  ) Pin(15, Pin.IN) (  ) Pin.LED(15) |
| `led.value(1)` will… | (  ) turn the LED on (  ) turn the LED off (  ) delete the LED |
| An ultrasonic sensor's Trig pin is set as an… | (  ) output (  ) input |

## Fill in the gaps
Complete these sentences.

A motor needs more power than the Pico can give, so we use a separate [[ ]] board. Every device on the breadboard needs a path back to [[ ]]. The long leg of an LED is called the [[ ]].

## Short answers
| Question | Your answer |
|---|---|
| Why does an ultrasonic sensor use the TIME an echo takes to come back? | Type your answer here |
| A reflective optical sensor reads a 1 over the black line and a 0 over the white floor. How does this let the buggy follow a line? | Type your answer here |
| "Synergy" means the parts working together. Give one example of synergy in your finished buggy. | Type your answer here |

## ✅ I can…
- [ ] I explained microcontrollers and embedded systems
- [ ] I told inputs from outputs
- [ ] I described the motors, the ultrasonic sensor and the line sensors
- [ ] I answered code and pin questions about the project
