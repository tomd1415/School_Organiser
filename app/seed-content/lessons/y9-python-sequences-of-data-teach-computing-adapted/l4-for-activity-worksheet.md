# The famous for — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use a `for` loop to go through a list, count matching items, collect them into a new list, and loop over a string.

## Two for-loop patterns  (we do)
Drag each line into the pattern it belongs to.

```sort
Counting pattern: count = 0, count = count + 1
Collecting pattern: selection = [], selection.append(dice)
```

## English words
This program loads a list of English words called `dictionary`.

![A Python program that loads a list of words and counts them with len]({{res:l4-english-words-code-teach-computing.png}})

| Question | Choose |
|---|---|
| What does `len(dictionary)` give? | (  ) how many words are in the list (  ) the first word (  ) the longest word |

## 🟢 Support
| Question | Choose |
|---|---|
| Which line goes through every word in the list? | (  ) for word in dictionary: (  ) for dictionary in word: (  ) while dictionary: |

## 🟡 Core
| Question | Your answer |
|---|---|
| Fill the gap so the loop goes through each word: `for word in [[ ]]:` | Type your answer here |
| Write a loop that prints every word in `dictionary`. | Type your code here |

## 🔴 Challenge — heartbeats
This program plots real heart (ECG) data. A heartbeat is a point where the value crosses from below zero to above zero (the red dots).

![A plotted ECG signal with red dots marking where the line crosses zero]({{res:l4-heartbeats-plot-teach-computing.png}})

| Question | Your answer |
|---|---|
| Write a loop header that goes through every `value` in `heartbeat_data`. | Type your code here |
| To spot a crossing you compare each value with the PREVIOUS one. Why must you store `previous = value` at the END of the loop? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste a link to your saved program here | Type your answer here |
| Show your finished program | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I used a `for` loop to go through a list
- [ ] I counted items that matched a condition
- [ ] I collected matching items into a new list
- [ ] I used a `for` loop over a string
