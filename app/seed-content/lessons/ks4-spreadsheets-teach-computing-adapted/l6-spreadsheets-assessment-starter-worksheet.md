# Spreadsheets assessment — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will show that I can use my spreadsheet skills on a new scenario.

## Starter — read the formula
Look at each formula and say what it does before the quiz.

| Question | Your answer |
|---|---|
| What does `=SUM(D8:D22)` do? | Type your answer here |

## 🟢 Support
Match each function to what it does. Drag the word onto its job.

| Job | Match the function |
|---|---|
| Adds up a range of cells | (  ) SUM (  ) IF (  ) COUNTIF (  ) VLOOKUP |
| Counts cells that match one thing | (  ) SUM (  ) IF (  ) COUNTIF (  ) VLOOKUP |
| Checks a condition and outputs a result | (  ) SUM (  ) IF (  ) COUNTIF (  ) VLOOKUP |
| Finds a value and returns matching data | (  ) SUM (  ) IF (  ) COUNTIF (  ) VLOOKUP |

## 🟡 Core
| Question | Your answer |
|---|---|
| What is a computer model, in your own words? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Give one real-life situation where a computer model is used to predict an outcome, and say why a model is useful there. | Type your answer here |
