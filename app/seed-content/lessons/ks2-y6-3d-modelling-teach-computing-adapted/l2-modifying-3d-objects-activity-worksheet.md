# Modifying 3D objects — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will resize a 3D object, lift it up and down, and change its colour.

## The handles
Different handles change a shape in different ways:
- the **white corner handles** change the **width and length** (two dimensions).
- the **black side handles** change **one** side (width or length).
- the **white top handle** changes the **height**.

![A cube with handles]({{res:l2-cube-handles.png}})

Drag each label onto the matching handle.

```label
image: {{res:l2-cube-handles.png}}
top (40%, 20%): Height handle
corner (18%, 50%): Corner handle
side (60%, 62%): Side handle
```

## 🟢 Support
| Question | Choose |
|---|---|
| Which handle changes the height of a shape? | (  ) the top handle (  ) a side handle (  ) the colour box |
| Dragging a handle will… | (  ) resize the shape (  ) delete the shape (  ) save your work |

## 🟡 Core
| Question | Your answer |
|---|---|
| You want to make a cube taller but keep the bottom the same size. Which handle do you drag? | Type your answer here |

## Lifting and lowering
The cone-shaped handle on top lets you **lift** a shape up or **lower** it down. It does not change the size. A shape sitting on the workplane is at **0 mm**.

![A triangular prism lifted onto a cuboid to make a house]({{res:l2-house.png}})

Put the steps in order to make this house shape.

```order
Add a cuboid for the walls
Add a triangular prism for the roof
Resize the prism to match the top of the cuboid
Lift the prism up so it sits on top of the cuboid
```

## 🔴 Challenge
| Question | Your answer |
|---|---|
| The roof needs to sit exactly on top of the walls. How does the lift number (in mm) help you put it in the right place? | Type your answer here |

## Changing colour
When a shape is selected, the colour box appears. Click it to recolour the shape.

![The Tinkercad colour picker]({{res:l2-colour-picker.png}})

| Question | Choose |
|---|---|
| How do you change the colour of a shape? | (  ) select it, then click the colour box (  ) drag a handle (  ) zoom in |

## Show your work
Make a house shape: resize and lift a roof onto a base, then recolour the parts.

| Question | Your answer |
|---|---|
| Show your finished house shape. | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I can use the handles on a 3D object
- [ ] I can resize an object in three dimensions
- [ ] I can lift and lower a 3D object
- [ ] I can recolour a 3D object
