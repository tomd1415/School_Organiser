# Network hardware — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will describe what each piece of network hardware does, define a MAC address, and choose the right hardware for a network.

A network needs hardware to move data around. Two important ones are below.

![A router — moves data from one network to another]({{res:l3-router.png}})
![A switch — sends data to the correct wired device]({{res:l3-switch.png}})

## Find the network port
A **NIC** (network interface card) lets a computer join a network. You plug a network cable into the **network port**. Drag each label onto the right port on this laptop.

```label
image: {{res:l3-laptop-ports.png}}
zone1 (17%, 73%): Network port (NIC)
zone2 (35%, 68%): HDMI port
zone3 (60%, 64%): USB port
```

## 🟢 Support
Match each piece of hardware to its job. Drag the device onto its job.

| What it does | Match the device |
|---|---|
| Joins one network to another — e.g. your network to the internet | (  ) Router (  ) Switch (  ) Hub (  ) NIC (  ) Wireless access point (  ) Bridge |
| Sends data only to the one device it is meant for | (  ) Router (  ) Switch (  ) Hub (  ) NIC (  ) Wireless access point (  ) Bridge |
| Copies incoming data and sends it to every device (broadcasts) | (  ) Router (  ) Switch (  ) Hub (  ) NIC (  ) Wireless access point (  ) Bridge |
| Lets a computer connect to a network — it has the MAC address | (  ) Router (  ) Switch (  ) Hub (  ) NIC (  ) Wireless access point (  ) Bridge |
| Gives devices a wireless (WiFi) way to join the network | (  ) Router (  ) Switch (  ) Hub (  ) NIC (  ) Wireless access point (  ) Bridge |
| Joins one LAN to another LAN to reduce traffic | (  ) Router (  ) Switch (  ) Hub (  ) NIC (  ) Wireless access point (  ) Bridge |

## 🟡 Core
| Question | Choose one |
|---|---|
| What is a MAC address? | (  ) a unique address built into a NIC that identifies a device on the network (  ) a web address you type into a browser (  ) a password for the WiFi |
| A switch sends data to just the right device. A **hub** does what instead? | (  ) broadcasts the data to every connected device (  ) joins two different networks (  ) gives out WiFi |

| Question | Your answer |
|---|---|
| Write what a **router** is used for. | Type your answer here |

## 🔴 Challenge
| Question | Choose one |
|---|---|
| A shop wants to connect several wired computers so that data only goes to the device it is meant for. Which device should they use? | (  ) a switch (  ) a hub (  ) a router |

| Question | Your answer |
|---|---|
| Explain **one** difference between a hub and a switch. | Type your answer here |
| A laptop with no wires needs to join the school WiFi. Which device makes that possible, and why? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Write the one device you found easiest to remember, and a way to remember it | Type your answer here |
| Show your finished sheet | 📷 Paste a photo of your finished work here |

## ✅ I can…
- [ ] I described what each piece of network hardware does
- [ ] I defined a MAC address
- [ ] I chose the right hardware for a network
