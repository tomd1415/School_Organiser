# Network topologies — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will describe the star, bus, mesh and ring topologies, give an advantage and disadvantage of each, and choose a topology for a scenario.

A **topology** is the *shape* — the way the nodes are arranged. Here are the four shapes.

![Star and bus topologies, with their advantages and disadvantages]({{res:l4-topologies.png}})

| Ring topology | Mesh topology |
|---|---|
| ![A ring topology — nodes joined in a circle]({{res:l4-ring-topology.png}}) | ![A mesh topology — many paths between nodes]({{res:l4-mesh-topology.png}}) |

## 🟢 Support
Use the four shapes above. Match each topology to how it is arranged. Drag the topology onto its description.

| How it is arranged | Match the topology |
|---|---|
| One central node (a hub or switch) with every other node joined to it | (  ) Star (  ) Bus (  ) Ring (  ) Mesh |
| Every node connects to one single cable, with a terminator at each end | (  ) Star (  ) Bus (  ) Ring (  ) Mesh |
| Each node is joined in a circle; data travels around in one direction | (  ) Star (  ) Bus (  ) Ring (  ) Mesh |
| Many paths between nodes; each node joins to one or more others | (  ) Star (  ) Bus (  ) Ring (  ) Mesh |

## 🟡 Core
Fill in the gap: a bus topology uses a [[ ]] at each end of the cable to stop the signal bouncing back.

| Question | Choose one |
|---|---|
| A research lab needs **three** computers connected for a **short, cheap, temporary** experiment, and security is not important. Which topology? | (  ) bus (  ) star (  ) mesh |
| Which topology has **one central node** that is a single point of failure? | (  ) star (  ) ring (  ) mesh |

## 🔴 Challenge
Drag each statement into the right box.

```sort
Star topology: there is one central node, the central node is a single point of failure, you can easily add a new node
Mesh topology: there is no central node so no single point of failure, data can take the most direct route, lots of cabling makes it expensive
```

| Question | Your answer |
|---|---|
| A busy office of 20 computers needs a **fast, stable** network where new devices can be added **without shutting the network down**, and they have a good budget. Choose a topology and give **one** reason. | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Write which topology you think is best for a small home, and why | Type your answer here |
| Show your finished sheet | 📷 Paste a photo of your finished work here |

## ✅ I can…
- [ ] I described the star, bus, mesh and ring topologies
- [ ] I gave an advantage and a disadvantage of a topology
- [ ] I chose a topology for a scenario
