# Algorithms review — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will sort and search a list, and compare the algorithms, ready for the assessment.

![A robot at a desk, revising with paper and pencil]({{res:l11-review-hook-teach-computing.png}})

## Sort the algorithms
Drag each algorithm under the job it does.

```sort
Searching algorithm: linear search, binary search
Sorting algorithm: bubble sort, insertion sort, merge sort
```

## The data
Idris has a list of songs in this order: **Shallow, Location, Sunflower, Giant, Wow., Senorita, Bad guy**

## 🟢 Support
| Question | Choose |
|---|---|
| Can you use a binary search on the song list above as it is now? | (  ) no — it is not in order yet (  ) yes — any list works (  ) only if it is short |
| Which search works on data that is NOT sorted? | (  ) linear search (  ) binary search (  ) neither |

## 🟡 Core
| Question | Choose |
|---|---|
| Which sort splits the list and merges it back together? | (  ) merge sort (  ) bubble sort (  ) insertion sort |
| Which sort compares neighbours and swaps them? | (  ) bubble sort (  ) merge sort (  ) binary search |

Put the steps for ONE pass of a bubble sort back in order.

```order
Start at the first item in the list
Compare the item to the one next to it
If it is bigger, swap the two items
Move to the next item
Repeat to the end of the list
```

## 🔴 Challenge
Drag each fact under the sorting algorithm it best describes.

```sort
Bubble sort: compares two neighbours at a time, simple but slow on big lists, sorts the items in place
Merge sort: splits the list into halves, merges sorted lists back together, fast on big lists
```

| Question | Your answer |
|---|---|
| Idris has a very large song list to sort. Explain whether bubble sort or merge sort is the better choice, and why. | Type your answer here |

## Show your work
Bubble sort the song list into order, then do a binary search to find "Shallow". Show your working.

| Question | Your answer |
|---|---|
| After sorting, which song is FIRST in the list? | Type your answer here |
| Show your sorted list and binary search | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I can choose the right searching or sorting algorithm for a list
- [ ] I can carry out a sort and then a search on a list
- [ ] I can describe how each algorithm works
- [ ] I can compare two algorithms and justify a choice
