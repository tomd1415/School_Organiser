# Binary mosaic — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will build a picture out of pixels and work out how many bits it needs.

## Predict
Zoom right in on any digital photo and you start to see small squares.

![A photo zoomed in until the pixels show]({{res:l1-pixels-zoom-astronaut.jpg}})

| Question | Your prediction |
|---|---|
| What are the small squares you can see? | Type your answer here |

## Key words — match each word to its meaning
| Word | Drop the matching meaning |
|---|---|
| pixel | (  ) one small square of an image (  ) the number of pixels in an image (  ) the number of bits for each pixel's colour |
| resolution | (  ) one small square of an image (  ) the number of pixels in an image (  ) the number of bits for each pixel's colour |
| colour depth | (  ) one small square of an image (  ) the number of pixels in an image (  ) the number of bits for each pixel's colour |

## 🟢 Support
| Question | Choose |
|---|---|
| A digital image is made of small squares called… | (  ) pixels (  ) waves (  ) bytes |
| The number of pixels in an image is its… | (  ) resolution (  ) volume (  ) sound |

> Tip: read each choice out loud before you pick one.

## 🟡 Core
Fill in each gap with a number.

A mosaic is 4 pixels wide and 3 pixels tall. It has [[ ]] pixels in total.

Each pixel uses 1 bit (black or white). The whole 4×3 image needs [[ ]] bits.

## 🔴 Challenge
| Question | Your answer |
|---|---|
| An image is 9 pixels wide and 10 pixels tall. Each pixel uses 3 bits. How many bits does the whole image need? Show your working. | Type your answer here |

## Calculation practice — bits in an image
Rule: **number of pixels × bits per pixel = total bits**.

| Question | Your answer |
|---|---|
| 10 × 10 pixels, 1 bit per pixel = ? bits | Type your answer here |
| 8 × 5 pixels, 2 bits per pixel = ? bits | Type your answer here |
| 9 × 10 pixels, 3 bits per pixel = ? bits | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste a link to your mosaic, or describe it here | Type your answer here |
| Show your finished mosaic | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described how an image is made of pixels
- [ ] I explained what resolution means
- [ ] I explained what colour depth means
- [ ] I worked out the number of bits in a small image
