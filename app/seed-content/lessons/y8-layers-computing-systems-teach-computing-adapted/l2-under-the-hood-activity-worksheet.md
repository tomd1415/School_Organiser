# Under the hood — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Every computer has the same main parts: a processor, memory, storage, input/output, and communication.

![A processor (CPU) chip — the part that runs the instructions]({{res:l2-processor-chip.png}})

## Match the part to its job
Drag each answer onto the matching part.

| Part | What it does |
|---|---|
| Processor (CPU) | (  ) runs the instructions (  ) holds data while working (  ) stores files when the power is off (  ) connects to other computers |
| Memory (RAM) | (  ) runs the instructions (  ) holds data while working (  ) stores files when the power is off (  ) connects to other computers |
| Storage | (  ) runs the instructions (  ) holds data while working (  ) stores files when the power is off (  ) connects to other computers |
| Communication | (  ) runs the instructions (  ) holds data while working (  ) stores files when the power is off (  ) connects to other computers |

## Sort it — input or output?
Drag each device into the right group.

```sort
Input: mouse, keyboard, microphone, camera
Output: screen, printer, speaker
```

## 🟢 Support
| Question | Choose one |
|---|---|
| Which part runs the program's instructions? | (  ) the processor (CPU) (  ) the speaker (  ) the case |

## 🟡 Core
Fill the gaps. Word bank: **processor · memory · instructions · data**

A computer contains a [[ ]] and some [[ ]].

The [[ ]] runs the program's instructions very quickly.

The memory holds both the instructions and the [[ ]] while the computer is working.

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Memory and storage both "hold" things. What is the difference between them? | Type your answer here |

## Label the device
This is a wearable computer (Google Glass). Drag each label onto the right part.

```label
image: {{res:l2-google-glass.jpg}}
camera (45%, 82%): camera
display (60%, 74%): display
touchpad (16%, 46%): touchpad
frame (68%, 18%): frame
```

## Show your work
| Question | Your answer |
|---|---|
| Write where your work is saved, or paste a link | Type your answer here |
| Show your finished work | 📷 Paste a screenshot or photo of your work here |

## ✅ I can…
- [ ] I named the main hardware parts
- [ ] I described what each part does
- [ ] I sorted input devices from output devices
- [ ] I explained that all computers have the same basic parts
