# It's only logical — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Logic gates follow truth tables. All hardware is built from logic gates, and uses 1s and 0s (binary).

## Match the operator to its meaning
Drag each meaning onto the right operator.

| Operator | What it means |
|---|---|
| NOT | (  ) gives the opposite (  ) true only if BOTH inputs are true (  ) true if AT LEAST ONE input is true |
| AND | (  ) gives the opposite (  ) true only if BOTH inputs are true (  ) true if AT LEAST ONE input is true |
| OR | (  ) gives the opposite (  ) true only if BOTH inputs are true (  ) true if AT LEAST ONE input is true |

## 🟢 Support
Complete the truth tables. Tick the right answer.

| Question | Choose one |
|---|---|
| NOT true = ? | (  ) false (  ) true |
| true AND true = ? | (  ) true (  ) false |

## 🟡 Core
| Question | Choose one |
|---|---|
| true AND false = ? | (  ) false (  ) true |
| false OR true = ? | (  ) true (  ) false |
| false OR false = ? | (  ) false (  ) true |

## 🔴 Challenge
| Question | Choose one |
|---|---|
| true AND (NOT false) = ? | (  ) true (  ) false |
| (NOT true) OR false = ? | (  ) false (  ) true |

## Hardware is built from logic gates
Even a part that adds two numbers is just lots of logic gates joined together.

![A circuit that adds binary digits, built from logic gates]({{res:l4-logic-circuit-adders.png}})

| Question | Tick ALL that are true |
|---|---|
| Tick everything that is true about logic gates. | [ ] gates use 1s and 0s [ ] the processor is built from gates [ ] gates can only store photos [ ] memory is built from gates |

## Show your work
| Question | Your answer |
|---|---|
| Paste a link to your AND-gate circuit, or write where it is saved | Type your answer here |
| Show your circuit | 📷 Paste a screenshot or photo of your work here |

## ✅ I can…
- [ ] I described the NOT, AND and OR operators
- [ ] I completed a truth table
- [ ] I explained that hardware is built from logic gates using 1s and 0s
