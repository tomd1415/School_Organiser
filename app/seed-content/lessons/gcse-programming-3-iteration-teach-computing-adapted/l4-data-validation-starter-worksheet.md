# Data validation — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will learn why programs need **data validation** — checking that the input is sensible.

## Starter — sort the data entry
A program asks: **"Enter a number between 1 and 10"** and then runs `number = int(input())`.

Sort each thing the user might type into the right group.

```sort
Correct: 7, 9, 10
Out of range: 12, 0, -5
ValueError: Five, 7.5, twelve
Empty string: Enter key pressed
```

## 🟢 Support
| Question | Choose |
|---|---|
| Data validation means… | (  ) checking the input is sensible (  ) deleting the program (  ) printing once |
| `7.5` causes a problem because it is… | (  ) not a whole number (an integer) (  ) too small (  ) empty |

## 🟡 Core
| Question | Your answer |
|---|---|
| Why might a program **crash** if it does not check the user's input? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| A **robust** program keeps working even with strange input. Give one reason robustness matters in a real program. | Type your answer here |
