# Swim database and assessment — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
First I will read SQL queries and predict what they do, before I finish the swim database.

## Starter — read the query
Read each query carefully. Say what it would do to the data.

| Question | Your prediction |
|---|---|
| DELETE FROM tblMembers WHERE MemberID = 41; — what does this do? | Type your answer here |

## 🟢 Support
Match each query word to what it does.

| Query word | What it does |
|---|---|
| SELECT | (  ) finds data (  ) adds data (  ) removes data (  ) changes data |
| INSERT | (  ) finds data (  ) adds data (  ) removes data (  ) changes data |
| DELETE | (  ) finds data (  ) adds data (  ) removes data (  ) changes data |
| UPDATE | (  ) finds data (  ) adds data (  ) removes data (  ) changes data |

## 🟡 Core
| Question | Your answer |
|---|---|
| UPDATE tblLessons SET CourseID = 2 WHERE CourseID = 1; — what will this change? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| DELETE FROM tblDownloads WHERE Date BETWEEN "2019-11-01" AND "2019-11-19"; — exactly which records are removed, and why is the WHERE line important? | Type your answer here |
