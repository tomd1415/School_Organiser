# Database essentials — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will describe a flat file database and a relational database, and explore a real database.

## Predict
Look at the flat file database on the board. The same names and titles appear again and again.

| Question | Your prediction |
|---|---|
| What problem could happen when the same data is stored over and over? | Type your answer here |

## Sort the facts
Drag each fact into the right kind of database.

```sort
Flat file database: Has one single table, Often repeats the same data, Can cause data inconsistency
Relational database: Has more than one table, Links tables using keys, Cuts down repeated data
```

## 🟢 Support
| Question | Choose |
|---|---|
| A flat file database has… | (  ) one table (  ) many tables (  ) no data |
| A relational database has… | (  ) more than one table (  ) only one row (  ) no fields |

## 🟡 Core
A relational database links two tables together using keys. Fill in the gaps.

A relational database links the primary key in one table to the [[ ]] key in another table. We do this to stop the same data being [[ ]] over and over.

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Explain "data redundancy" in your own words. | Type your answer here |
| Explain "data inconsistency" using the repeated music data as an example. | Type your answer here |

## Explore the database software
Open your COPY of dbMusic.db in DB Browser for SQLite. The Database Structure tab lists the tables.

![The Database Structure tab in DB Browser for SQLite]({{res:l1-db-browser-structure.png}})

| Question | Your answer |
|---|---|
| How many tables can you see in dbMusic.db? | Type your answer here |
| Write the name of one table. | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Write the name of the table you explored | Type your answer here |
| Show your database open in DB Browser | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what a database is
- [ ] I named the parts: table, record, field, primary key
- [ ] I described a flat file database
- [ ] I described a relational database
