# Final project — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## Starter — write the HTML and CSS
Look at the extract on the board: a heading, then a bullet-point list of three films, inside a section called `my_list`.

| Question | Your answer |
|---|---|
| Write the HTML for that section. Put the bullet list inside a `<div class="my_list">`. (You can assume it is already inside `<body>`.) | Type your code here |

## 🟢 Support
| Question | Choose |
|---|---|
| A bullet-point list is made with which tag? | (  ) `<ul>` (  ) `<ol>` (  ) `<p>` |
| A class is added to a div like this… | (  ) `<div class="my_list">` (  ) `<div my_list>` (  ) `<class=my_list>` |

## 🟡 Core
| Question | Your answer |
|---|---|
| Write the CSS rule that makes the `my_list` text red. | Type your code here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Add to your CSS rule so the list is also shown in a monospace font. | Type your code here |
