# Images and links — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will add an image with the `img` tag and make a hyperlink with the `a href` tag.

## Predict
A picture of a dog is saved online.

![Two photo frames standing for images on a page]({{res:l2-image-frames-teach-computing.png}})

| Question | Your prediction |
|---|---|
| The tag is `<img src="dog.jpg" alt="My dog">`. What will show on the page? | Type your answer here |

## Match the img tag part to its job
Drag each answer to the right part.

| Part of the tag | What it does |
|---|---|
| `src` | (  ) where the image file is (  ) text read out by a screen reader (  ) how wide the image is |
| `alt` | (  ) where the image file is (  ) text read out by a screen reader (  ) how wide the image is |
| `width` | (  ) where the image file is (  ) text read out by a screen reader (  ) how wide the image is |

## 🟢 Support
Sort each item into the right group.

```sort
Tag: <img>, <a>, <p>
Attribute: src, alt, href, width
```

## 🟡 Core
Put the steps to add an image in the correct order.

```order
Find a suitable image and copy its address
Write an <img> tag where you want the image
Set src to the image's address
Add alt text that describes the image
Open the page in a browser to check it
```

| Question | Your answer |
|---|---|
| Why must `src` include the file name, like `dog.jpg`? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write a full hyperlink to `www.bbc.co.uk` with the link text "BBC". | Type your code here |
| Give one reason alt text still matters even when the image loads correctly. | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste the HTML you wrote here | Type your code here |
| Show your page with an image and a link | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I explained what accessibility means
- [ ] I added an image with the `img` tag
- [ ] I created a hyperlink with the `a href` tag
