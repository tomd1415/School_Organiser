# Introduction to HTML — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use HTML tags to structure a web page with headings, paragraphs and lists.

## Predict
Look at the code on the board before it opens in the browser.

| Question | Your prediction |
|---|---|
| What will `<h1>My favourite things</h1>` look like on the page? | Type your answer here |

## Match the tag to what it does
Drag each answer to the right tag.

| Tag | What it does |
|---|---|
| `<h1>` | (  ) a big heading (  ) a paragraph of text (  ) a bullet-point list (  ) a numbered list |
| `<p>` | (  ) a big heading (  ) a paragraph of text (  ) a bullet-point list (  ) a numbered list |
| `<ul>` | (  ) a big heading (  ) a paragraph of text (  ) a bullet-point list (  ) a numbered list |
| `<ol>` | (  ) a big heading (  ) a paragraph of text (  ) a bullet-point list (  ) a numbered list |

## 🟢 Support
A real page made of heading, list and paragraph tags:

![HTML code that uses heading, list and paragraph tags]({{res:l1-html-lists-code-teach-computing.png}})

| Question | Choose |
|---|---|
| An HTML tag is written inside which brackets? | (  ) < > (  ) ( ) (  ) { } |
| A closing tag is different from an opening tag because it has a… | (  ) / slash (  ) * star (  ) ! mark |

## 🟡 Core
Put the HTML lines in the correct order to make a valid web page.

```parsons
<!DOCTYPE html>
<html>
<body>
<h1>My favourite things</h1>
<p>Here are some things I like.</p>
</body>
</html>
```

| Question | Your answer |
|---|---|
| What is the difference between a `<ul>` list and an `<ol>` list? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write the HTML for a bullet-point list of three foods you like. | Type your code here |
| Why is it useful that an HTML page is just plain text? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste the HTML you wrote here | Type your code here |
| Show your web page in the browser | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what HTML is and what a tag does
- [ ] I used heading, paragraph and list tags to structure a page
- [ ] I wrote and viewed a simple HTML page in a browser
