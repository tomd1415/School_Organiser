# DIVs and classes — activity worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## What we are learning
Today I will use `<div>` tags with classes to break a page into sections and style them with CSS.

## Predict
Here is HTML with two `<div>` sections, and the page it makes.

![Two div sections styled as blue boxes, with the code beside the output]({{res:l5-two-styled-divs-code-and-output-teach-computing.png}})

| Question | Your prediction |
|---|---|
| Both `<div>` sections use `class="favourite"`. How will the two boxes look compared to each other? | Type your answer here |

## Match the word to its meaning
Drag each answer to the right word.

| Word | What it means |
|---|---|
| `div` | (  ) a section of a page (  ) a name you give a section (  ) a div inside another div |
| `class` | (  ) a section of a page (  ) a name you give a section (  ) a div inside another div |
| nesting | (  ) a section of a page (  ) a name you give a section (  ) a div inside another div |

## 🟢 Support
| Question | Choose |
|---|---|
| A class is added to a div like this… | (  ) `<div class="header">` (  ) `<class div="header">` (  ) `<div-class=header>` |
| In CSS, a class rule starts with a… | (  ) full stop, like `.header` (  ) hash, like `#header` (  ) star, like `*header` |

## 🟡 Core
Put the lines in order to make one valid `<div>` section with a class.

```parsons
<div class="header">
<h1>My website</h1>
<p>Welcome to my page.</p>
</div>
```

| Question | Your answer |
|---|---|
| Why are `<div>` tags useful when styling a big page? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| Write the CSS for a class called `header` that gives it a yellow background. | Type your code here |
| What is a "nested" div, and why might you use one? | Type your answer here |

## Show your work
| Question | Your answer |
|---|---|
| Paste the HTML and CSS you wrote here | Type your code here |
| Show your styled sections in the browser | 📷 Paste a screenshot of your work here |

## ✅ I can…
- [ ] I described what a `<div>` is used for
- [ ] I gave a `<div>` a class
- [ ] I used a class in CSS to style a section
