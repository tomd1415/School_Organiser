# Crunching numbers — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## Starter — what do these symbols do?
Match each Python operator to what it does.

| Operator | What it does |
|---|---|
| + | (  ) add (  ) subtract (  ) multiply (  ) whole-number divide |
| - | (  ) add (  ) subtract (  ) multiply (  ) whole-number divide |
| * | (  ) add (  ) subtract (  ) multiply (  ) whole-number divide |
| // | (  ) add (  ) subtract (  ) multiply (  ) whole-number divide |

## Predict
Read these two lines, then answer.

`number = 5`
`double = 2 * number`
`number = 15`

| Question | Choose |
|---|---|
| After all three lines run, what is the value of `double`? | (  ) 10 (  ) 30 (  ) you cannot change number again |

## 🟢 Support
| Question | Choose |
|---|---|
| Python reads an assignment line… | (  ) right to left (work out the answer, then store it) (  ) left to right (  ) top to bottom only |

## 🟡 Core
| Question | Your answer |
|---|---|
| `double` is set to 10, then `number` changes to 15. Why is `double` still 10? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| After `number = 5` then `number = number + 10`, what is `number`? Explain how Python works it out. | Type your answer here |
