# Putting it all together — starter worksheet

| Name | Type your name here |
|---|---|
| Date | Type the date here |

## Starter — which parts repeat?
In a guessing game, some steps happen once and some repeat. Drag each step into the right group.

```sort
Happens once (before the loop): Pick the lucky number
Repeats inside the loop: Ask the user to guess, Show the user feedback
```

## 🟢 Support
| Question | Choose |
|---|---|
| A Boolean variable can hold… | (  ) True or False (  ) any number (  ) any word |

## 🟡 Core
| Question | Your answer |
|---|---|
| Why should "pick the lucky number" happen only once, not every round? | Type your answer here |

## 🔴 Challenge
| Question | Your answer |
|---|---|
| A flag starts as `False` and is set to `True` when the user wins. What happens to the loop if the flag is never set to `True`? | Type your answer here |
